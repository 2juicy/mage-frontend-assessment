export interface Student {
  city: string;
  company: string;
  email: string;
  firstName: string;
  lastName: string;
  grades: Array<any>;
  id: string;
  pic: string;
  skill: string;
  show: boolean;
  tags: Array<string>;
}
