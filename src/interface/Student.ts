export interface Student {
  city: string;
  company: string;
  email: string;
  firstName: string;
  lastName: string;
  grades: Array<number>;
  id: string;
  pic: string;
  skill: string;
  show: boolean;
  tags: Array<string>;
}
